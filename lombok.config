# is telling Lombok that this is the root directory and that it shouldn’t search parent directories for more configuration files
config.stopBubbling = true
# make sonar happy: is telling Lombok to add the @lombok.Generated annotation to all generated methods.
lombok.addLombokGeneratedAnnotation = true