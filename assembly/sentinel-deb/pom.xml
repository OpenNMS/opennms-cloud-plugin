<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.opennms.plugins.cloud</groupId>
        <artifactId>assembly</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.opennms.plugins.cloud.assembly</groupId>
    <artifactId>org.opennms.plugins.cloud.assembly.sentinel.deb</artifactId>
    <name>OpenNMS :: Plugins :: Cloud :: Assembly :: Sentinel Deb</name>
    <packaging>pom</packaging>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>net.sf.debian-maven</groupId>
                    <artifactId>debian-maven-plugin</artifactId>
                    <version>1.0.6</version>
                    <executions>
                        <execution>
                            <id>generate-deb</id>
                            <goals>
                                <goal>package</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <targetDir>${project.build.directory}</targetDir>
                        <packageName>sentinel-cloud-plugin</packageName>
                        <packageVersion>${project.version}</packageVersion>
                        <packageRevision>1</packageRevision>
                        <packageSection>contrib/net</packageSection>
                        <packageTitle>OpenNMS :: Plugins :: Cloud Plugin</packageTitle>
                        <packageDescription>opennms-cloud-plugin</packageDescription>
                        <packageDependencies>
                            <packageDependency>opennms-sentinel (&gt;= 30.0.0)</packageDependency>
                        </packageDependencies>
                        <excludeAllArtifacts>true</excludeAllArtifacts>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.1.1</version>
                <configuration>
                    <attach>false</attach>
                    <appendAssemblyId>false</appendAssemblyId>
                    <outputDirectory>${project.build.directory}/releases/</outputDirectory>
                    <descriptors>
                        <descriptor>${basedir}/src/main/assemblies/plugin.xml</descriptor>
                    </descriptors>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.coderplus.maven.plugins</groupId>
                <artifactId>copy-rename-maven-plugin</artifactId>
                <version>1.0.1</version>
                <executions>
                    <execution>
                        <id>copy-kar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>rename</goal>
                        </goals>
                        <configuration>
                            <sourceFile>
                               ${project.build.directory}/releases/${project.artifactId}-${project.version}/opennms-cloud-plugin/org.opennms.plugins.cloud.assembly.kar-${project.version}.kar
                            </sourceFile>
                            <destinationFile>
                                ${project.build.directory}/deb/usr/share/sentinel/deploy/opennms-cloud-plugin.kar
                            </destinationFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>build.debs</id>
            <activation>
                <activeByDefault>false</activeByDefault>
                <file>
                    <exists>/usr/bin/dpkg-buildpackage</exists>
                </file>
                <property>
                    <name>build.packages</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>net.sf.debian-maven</groupId>
                        <artifactId>debian-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>org.opennms.plugins.cloud.assembly</groupId>
            <artifactId>org.opennms.plugins.cloud.assembly.kar</artifactId>
            <version>${project.version}</version>
            <type>kar</type>
        </dependency>
    </dependencies>
</project>
